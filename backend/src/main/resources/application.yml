server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/luban?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: cyj2625130
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  security:
    debug: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

app:
  jwt:
    secret: luban-jwt-secret-key-change-in-production-256-bit
    expiration: 86400000

luban:
  mock-mes:
    enabled: true
  agent:
    rate-limit:
      max-requests: 30
      window-minutes: 1
  drivers:
    storage-path: drivers
    auto-load-on-startup: true
    maven:
      repo-url: https://maven.aliyun.com/repository/central
      fallback-url: https://repo.huaweicloud.com/repository/maven/
      connect-timeout: 10000
      read-timeout: 30000
      max-retries: 3
  datasource:
    secret: ${LUBAN_DATASOURCE_SECRET:s5NHFCgkA7ju+WVrOFtZzG/0SQZRnaMggynl9q9fze4=}