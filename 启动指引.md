# 鲁班（Luban）启动指引

## 一、从 Docker 启动（推荐）

无需安装 Java、Maven、Node.js、Python、MySQL 等任何环境，只需安装 [Docker](https://www.docker.com/products/docker-desktop/)。

提供两种方式：

| 方式 | 适用场景 | 说明 |
|------|---------|------|
| **Docker Compose** | 开发调试、多台机器部署 | 4 个独立镜像，可单独升级 |
| **All-in-One** | 快速体验、别人分发的镜像 | 1 个镜像包含全部服务 |

两种方式启动后使用完全一致，访问 `http://localhost`，默认账号 **`root@luban.local`** / **`123456`**。

---

### 1.1 Docker Compose（多镜像分离部署）

适合自己开发调试，各服务可独立重启和升级。

```bash
# 1. 进入项目目录
cd luban

# 2. 复制环境变量配置（可选，默认值即可使用）
cp .env.docker .env

# 3. 一键构建并启动
docker-compose up -d --build

# 4. 查看日志
docker-compose logs -f
```

服务架构：

```
docker-compose.yml
├── mysql (3306)          ← MySQL 8.0，带健康检查
├── embedding (8765)      ← Python Flask，BGE 中文向量模型
├── backend (8080)        ← Spring Boot 3.3 + Java 21
└── frontend (80)         ← Nginx 托管 Vite 构建产物
```

所有数据通过 Docker Volume 持久化，重启不丢失。密钥首次启动自动生成并持久化，后续重启自动复用。

常用命令：

```bash
docker-compose stop          # 停止所有服务
docker-compose start         # 重新启动
docker-compose logs -f 服务名  # 查看指定服务日志
docker-compose down -v       # 彻底删除（含所有数据）
```

---

### 1.2 All-in-One 单镜像（直接启动）

适合拿到别人打包好的镜像，一行命令启动。

**前提：** 已通过 `docker pull` 拉取镜像，或从 `docker load -i xxx.tar` 加载。

```bash
# 启动（注意替换数据目录为你电脑上的真实路径）
docker run -d --name luban -p 80:80 -v luban-data:/app/data luban:latest
#                                 ↑          ↑
#                           宿主机端口    数据卷名（可改，如 /home/user/luban:/app/data）
```

**macOS 示例（数据存到桌面）：**

```bash
docker run -d --name luban \
  -p 80:80 \
  -v ~/Desktop/luban-data:/app/data \
  luban:latest
```

**Windows 示例（数据存到 D 盘）：**

```powershell
docker run -d --name luban `
  -p 80:80 `
  -v D:\luban-data:/app/data `
  luban:latest
```

> 不指定路径直接用 `luban-data` 也行，Docker 会自动管理，只是数据位置不直观。

```bash
# 查看日志
docker logs -f luban
```

镜像内部结构：

```
一个容器 = 四个服务（supervisord 统一管理）
├── MySQL 8.0      (3306)  ← 数据保存在 /app/data/mysql
├── Embedding 服务  (8765)  ← BGE 中文向量模型（已内置）
├── Spring Boot    (8080)  ← Java 21，JPA 自动建表
└── Nginx           (80)   ← 前端 + /api 反向代理到后端
```

常用命令：

```bash
docker stop luban       # 停止
docker start luban      # 重启（数据不丢失，密钥不变）
docker exec -it luban bash  # 进入容器排查
docker rm -f luban && docker volume rm luban-data  # 彻底删除
```

---

### 1.3 启动后配置

1. 打开浏览器访问 `http://localhost`
2. 使用默认账号登录：**`root@luban.local`** / **`123456`**
3. ⚠️ **首次登录后请立即修改密码**（右上角用户名 → 修改密码）
4. 配置大模型（必需）：左侧菜单「系统配置」→「大模型配置」→ 编辑默认配置，填写：

| 字段 | 说明 | 示例 |
|------|------|------|
| 模型端点 | LLM API 地址（OpenAI 兼容格式） | `https://ark.cn-beijing.volces.com/api/plan/v3` |
| API Key | 你的 API 密钥 | `ark-xxx` 或 `sk-xxx` |
| 模型名称 | 模型 ID | `doubao-seed-2.0-lite` 或 `deepseek-chat` |

5. 点击「测试连接」验证 → 「保存」
6. 点击顶部「新建应用」，创建第一个应用

> 支持任意 OpenAI 兼容接口，包括 DeepSeek、豆包（火山方舟）、OpenAI 等。

---

## 二、从源码启动

适合需要在本地开发、调试、修改源码的场景。

### 2.1 环境要求

| 中间件/工具 | 版本要求 | 说明 |
|------------|---------|------|
| **MySQL** | 8.0 | 数据存储 |
| **Java** | 21+ | 后端运行环境 |
| **Maven** | 3.6+ | 后端构建工具 |
| **Node.js** | 22+ | 前端运行环境 |
| **npm** | 10+ | 前端包管理器 |

> 本项目不依赖 Redis、ElasticSearch 等额外中间件，仅需 MySQL 即可运行。

---

### 2.2 启动 MySQL

**方式一：Docker（推荐，一键启动）**

```bash
docker run -d --name luban-mysql \
  -e MYSQL_ROOT_PASSWORD=luban123 \
  -e MYSQL_DATABASE=luban \
  -p 3306:3306 \
  mysql:8.0 \
  --default-authentication-plugin=mysql_native_password \
  --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
```

**方式二：本地 MySQL**

手动创建数据库：

```sql
CREATE DATABASE luban CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

---

### 2.3 配置数据库连接

编辑 `backend/src/main/resources/application.yml`，修改数据库用户名和密码：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/luban?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: luban123
```

---

### 2.4 启动后端

```bash
cd backend

# 推荐：使用启动脚本（自动管理密钥，重启后登录态不丢失）
./start.sh

# 或直接 Maven 启动
mvn spring-boot:run
```

`start.sh` 会自动完成：
- 生成并持久化 `LUBAN_JWT_SECRET`（JWT 签名密钥）
- 生成并持久化 `LUBAN_DATASOURCE_SECRET`（数据源密码加密密钥）
- 生成并持久化 RSA 私钥（传输层信封加密）

密钥存储在 `backend/.env` 和 `backend/.rsa_key.pem`，首次启动自动生成，后续复用。

> 如果使用 `mvn spring-boot:run` 直接启动，每次重启 JWT 密钥会变化，导致所有用户登录态失效。

后端启动后访问 `http://localhost:8080`，JPA 自动建表（`ddl-auto: update`），无需手动执行 SQL。

---

### 2.5 启动前端

```bash
cd frontend
npm install
npm run dev
```

前端开发服务器运行在 `http://localhost:5173`，API 自动代理到 `http://localhost:8080`。

---

### 2.6 默认账号

系统首次启动后自动创建超管账号：

| 邮箱 | 密码 | 角色 |
|------|------|------|
| `root@luban.local` | `123456` | 超级管理员（全部权限） |

> ⚠️ **首次登录后请立即修改密码。**

注册新账号：访问 `http://localhost:5173/register`。

---

### 2.7 首次使用配置

同 [1.3 启动后配置](#13-启动后配置)，登录后配置大模型即可使用。

---

### 2.8 端口说明

| 服务 | 端口 | 访问地址 |
|------|------|---------|
| 前端开发服务器 | 5173 | http://localhost:5173 |
| 后端 API | 8080 | http://localhost:8080 |
| MySQL | 3306 | localhost:3306 |

---

### 2.9 常见问题

**Q: 启动后端报错 `Access denied for user`**
A: 检查 `application.yml` 中数据库用户名密码是否正确，Docker 默认密码为 `luban123`。

**Q: 前端请求后端报 401**
A: JWT 密钥变化导致登录态失效，重新登录即可。建议使用 `start.sh` 启动。

**Q: 大模型配置测试连接报 404**
A: 模型端点填写 API 前缀（如 `https://ark.cn-beijing.volces.com/api/plan/v3`），后端自动拼接 `/chat/completions`。

**Q: 数据源密码保存后解密失败**
A: RSA 密钥未持久化导致重启后变化。使用 `start.sh` 启动可自动持久化密钥。

---

## 三、附录：如何打包分发

本章说明如何将鲁班打包成 Docker 镜像，分发给其他人使用。

---

### 3.1 打包成 All-in-One 单镜像

将 MySQL、Embedding、后端、前端全部打包成**一个镜像**。
BGE 向量模型在构建时从网络下载到镜像中，用户拿到镜像后无需联网。
Python 脚本执行使用直接 subprocess 模式（沙箱模式自动降级，Docker 容器内默认关闭）。

**构建镜像：**

```bash
# 在项目根目录执行
docker build -f Dockerfile.allinone -t luban:latest .
```

或使用一键脚本（构建 + 自动导出 tar）：

```bash
bash scripts/build-allinone.sh
```

---

### 3.2 分发方式

#### 方式 A：上传到 Docker Hub

> 需要注册 [Docker Hub](https://hub.docker.com/) 账号（免费，注册后在本机执行 `docker login` 登录）。
> 
> 国内替代方案：**阿里云容器镜像服务**（[cr.console.aliyun.com](https://cr.console.aliyun.com)）免费额度，速度更快。操作步骤：
> 
> 1. 打开 [cr.console.aliyun.com](https://cr.console.aliyun.com)，登录阿里云账号
> 2. 左侧「实例列表」→「个人版」→ 创建**命名空间**（如 `luban`）
> 3. 左侧「访问凭证」→ 设置**固定密码**（这个密码专门用于 docker login，不是阿里云账号密码）
> 4. 本机执行 `docker login`：
> ```bash
> docker login --username=你的阿里云账号 registry.cn-hangzhou.aliyuncs.com
> # 输入第 3 步设置的固定密码
> ```
> 5. 推送镜像：
> ```bash
> docker tag luban:latest registry.cn-hangzhou.aliyuncs.com/chengyj90/luban:latest
> docker push registry.cn-hangzhou.aliyuncs.com/chengyj90/luban:latest
> ```
> 对方拉取：`docker pull registry.cn-hangzhou.aliyuncs.com/chengyj90/luban:latest`

```bash
# 打标签（替换为你的用户名）
docker tag luban:latest 你的用户名/luban:latest

# 推送
docker push 你的用户名/luban:latest
```

对方启动：

```bash
docker pull 你的用户名/luban:latest
# 启动命令见 1.2 节，有 macOS / Windows 示例
docker run -d --name luban -p 80:80 -v luban-data:/app/data 你的用户名/luban:latest
```

#### 方式 B：导出 tar 文件（U 盘 / 网盘）

```bash
# 导出为 tar 文件（约 1.5~2GB）
docker save -o luban-image.tar luban:latest
```

将 `luban-image.tar` 拷贝给对方。对方操作：

```bash
docker load -i luban-image.tar
# 启动命令见 1.2 节，有 macOS / Windows 示例
docker run -d --name luban -p 80:80 -v luban-data:/app/data luban:latest
```

---

### 3.3 打包 Docker Compose 多镜像

如果需要分发多镜像版本（各服务独立镜像），可以导出全部镜像：

```bash
# 先构建所有镜像
docker-compose build

# 导出全部镜像到一个 tar 文件
docker save -o luban-compose.tar \
  mysql:8.0 \
  luban-embedding:latest \
  luban-backend:latest \
  luban-frontend:latest
```

将 `luban-compose.tar` 和 `docker-compose.yml`、`.env.docker` 一起发给对方。对方操作：

```bash
docker load -i luban-compose.tar
cp .env.docker .env
docker-compose up -d
```

---

### 3.4 相关文件一览

| 文件 | 用途 |
|------|------|
| `Dockerfile.allinone` | All-in-One 单镜像构建文件 |
| `scripts/build-allinone.sh` | 一键构建 + 导出 tar |
| `scripts/docker-entrypoint.sh` | All-in-One 启动脚本（初始化 MySQL、生成密钥） |
| `scripts/supervisord.conf` | 进程管理器配置 |
| `scripts/nginx.conf` | Nginx 配置 |
| `docker-compose.yml` | 多镜像编排文件 |
| `.env.docker` | 环境变量模板（复制为 `.env` 使用） |
| `backend/Dockerfile` | 后端独立镜像 |
| `frontend/Dockerfile` | 前端独立镜像 |
| `embedding-service/Dockerfile` | Embedding 服务独立镜像 |