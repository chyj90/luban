# 鲁班（Luban）启动指引

## 零、Docker 一键部署（推荐分发方式）

如果你想把鲁班打包分发给其他人使用，或者不想手动安装 Java、Maven、Node.js 等环境，推荐使用 Docker。

提供两种方式：

| 方式 | 适用场景 | 镜像数量 |
|------|---------|---------|
| **Docker Compose**（多镜像） | 开发调试、多台机器部署 | 4 个（MySQL + Embedding + 后端 + 前端） |
| **All-in-One 镜像**（单镜像） | 分发给别人、U 盘拷贝、Docker Hub 发布 | 1 个（包含全部服务） |

---

### 方式一：Docker Compose（多镜像分离部署）

适合自己开发调试，或需要在多台机器上部署的场景。

```bash
# 1. 进入项目目录
cd luban

# 2. 复制环境变量配置（可选，默认值即可使用）
cp .env.docker .env

# 3. 一键构建并启动所有服务
docker-compose up -d --build

# 4. 查看日志
docker-compose logs -f

# 5. 访问
# 前端: http://localhost
# 后端: http://localhost:8080
```

服务架构：

```
docker-compose.yml
├── mysql (3306)          ← MySQL 8.0，带健康检查
├── embedding (8765)      ← Python Flask，BGE 中文向量模型
├── backend (8080)        ← Spring Boot 3.3 + Java 21
└── frontend (80)         ← Nginx 托管 Vite 构建产物
```

所有数据通过 Docker Volume 持久化，重启不丢失。

---

### 方式二：All-in-One 单镜像（打包分发）

将 MySQL、Embedding、后端、前端全部打包成**一个镜像**，适合分发给其他人使用。

#### 构建镜像

```bash
# 一键构建并导出 tar 文件
bash scripts/build-allinone.sh

# 或手动构建
docker build -f Dockerfile.allinone -t luban:latest .
```

#### 分发给其他人

**A. 上传到 Docker Hub**

```bash
# 打标签并推送
docker tag luban:latest 你的用户名/luban:latest
docker push 你的用户名/luban:latest
```

对方只需一行命令即可运行：

```bash
docker run -d --name luban -p 80:80 -v luban-data:/app/data 你的用户名/luban:latest
```

**B. U 盘拷贝**

```bash
# 导出为 tar 文件（约 1.5~2GB）
docker save -o luban-image.tar luban:latest

# 将 luban-image.tar 拷贝到 U 盘
```

对方拿到 U 盘后：

```bash
# 加载镜像
docker load -i luban-image.tar

# 启动（一行命令）
docker run -d --name luban -p 80:80 -v luban-data:/app/data luban:latest
```

**C. 本地运行**

```bash
docker run -d --name luban -p 80:80 -v luban-data:/app/data luban:latest
```

#### 镜像内部结构

```
一个容器 = 四个服务（supervisord 统一管理）
├── MySQL 8.0      (3306)  ← 数据保存在 /app/data/mysql
├── Embedding 服务  (8765)  ← BGE 中文向量模型
├── Spring Boot    (8080)  ← Java 21，JPA 自动建表
└── Nginx           (80)   ← 前端 + /api 反向代理到后端
```

#### 常用 Docker 命令

```bash
# 查看日志
docker logs -f luban

# 进入容器
docker exec -it luban bash

# 停止
docker stop luban

# 重新启动（数据不丢失）
docker start luban

# 删除容器和所有数据
docker rm -f luban
docker volume rm luban-data
```

---

## 一、环境要求

| 中间件/工具 | 版本要求 | 说明 |
|------------|---------|------|
| **MySQL** | 8.0 | 数据存储 |
| **Java** | 21+ | 后端运行环境 |
| **Maven** | 3.6+ | 后端构建工具 |
| **Node.js** | 22+ | 前端运行环境 |
| **npm** | 10+ | 前端包管理器 |

> 本项目不依赖 Redis、ElasticSearch 等额外中间件，仅需 MySQL 即可运行。

---

## 二、启动 MySQL

### 方式一：Docker（推荐，一键启动）

```bash
cd luban
docker-compose up -d
```

自动创建 `luban` 数据库，root 密码为 `luban123`，端口 `3306`。

### 方式二：本地 MySQL

手动创建数据库：

```sql
CREATE DATABASE luban CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

---

## 三、配置数据库连接

编辑 `backend/src/main/resources/application.yml`，修改数据库用户名和密码：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/luban?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: 你的密码   # Docker 默认 luban123
```

> 如果使用 Docker，`username` 为 `root`，`password` 为 `luban123`。

---

## 四、启动后端

```bash
cd backend

# 方式一：使用启动脚本（推荐，自动管理密钥）
./start.sh

# 方式二：直接 Maven 启动
mvn spring-boot:run
```

**推荐使用 `start.sh`**，它会自动完成以下工作：

- 生成并持久化 `LUBAN_JWT_SECRET`（JWT 签名密钥，重启后登录态不丢失）
- 生成并持久化 `LUBAN_DATASOURCE_SECRET`（数据源密码加密密钥）
- 生成并持久化 RSA 私钥（传输层信封加密，用于保护数据源密码等敏感字段）

密钥存储在 `backend/.env` 和 `backend/.rsa_key.pem`，首次启动自动生成，后续启动复用。

> **注意**：如果使用 `mvn spring-boot:run` 直接启动，每次重启后 JWT 密钥会变化，导致所有用户登录态失效，需要重新登录。

后端启动后访问 `http://localhost:8080`。JPA 会在首次启动时自动建表（`ddl-auto: update`），无需手动执行 SQL。

---

## 五、启动前端

```bash
cd frontend
npm install
npm run dev
```

前端开发服务器运行在 `http://localhost:5173`，API 请求会自动代理到 `http://localhost:8080`。

---

## 六、创建第一个账号

系统首次启动后会自动创建超管账号：

| 邮箱 | 密码 | 角色 |
|------|------|------|
| `root@luban.local` | `123456` | 超级管理员（全部权限） |

> ⚠️ **首次登录后请立即修改密码。** 登录后点击右上角用户名 → 「修改密码」。

### 注册新账号

访问 `http://localhost:5173/register`，填写账号、邮箱、密码即可注册。新注册用户默认为普通用户角色。

---

## 七、首次使用配置

### 1. 登录

访问 `http://localhost:5173/login`，使用 `root@luban.local` / `123456` 登录。

> 注意：登录使用**邮箱**作为凭证，不是账号名。密码传输全程 RSA 加密，无需担心明文泄露。

### 2. 配置大模型（必需）

开发助手依赖大模型能力，首次使用必须配置：

1. 点击左侧菜单「系统配置」→「大模型配置」
2. 点击已有配置的「编辑」按钮
3. 填写以下信息：

| 字段 | 说明 | 示例 |
|------|------|------|
| 模型端点 | LLM API 地址（OpenAI 兼容格式） | `https://ark.cn-beijing.volces.com/api/plan/v3` |
| API Key | 你的 API 密钥 | `ark-xxx` 或 `sk-xxx` |
| 模型名称 | 模型 ID | `doubao-seed-2.0-lite` 或 `deepseek-chat` |

4. 点击「测试连接」验证配置是否正确
5. 点击「保存」

> 支持任意 OpenAI 兼容接口，包括 DeepSeek、豆包（火山方舟）、OpenAI 等。

### 3. 创建应用

1. 点击顶部「新建应用」
2. 输入应用名称，创建第一个应用

---

## 八、端口说明

| 服务 | 端口 | 访问地址 |
|------|------|---------|
| 前端开发服务器 | 5173 | http://localhost:5173 |
| 后端 API | 8080 | http://localhost:8080 |
| MySQL | 3306 | localhost:3306 |

---

## 九、常见问题

### Q: 启动后端报错 `Access denied for user`

A: 检查 `application.yml` 中的数据库用户名和密码是否正确。Docker 默认密码为 `luban123`。

### Q: 前端请求后端报 401

A: JWT 密钥变化导致登录态失效，重新登录即可。建议使用 `start.sh` 启动以避免密钥变化。

### Q: 大模型配置测试连接报 404

A: 模型端点填写的是 API 前缀（如 `https://ark.cn-beijing.volces.com/api/plan/v3`），后端会自动拼接 `/chat/completions`。如果端点本身已包含 `/chat/completions`，直接填写完整 URL 即可。

### Q: 数据源密码保存后解密失败

A: 通常是因为 RSA 密钥未持久化导致重启后密钥变化。使用 `start.sh` 启动可自动持久化密钥。如果手动启动，需确保 `backend/.rsa_key.pem` 文件存在且不被删除。