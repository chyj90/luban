The user wants me to reorder the test cases so they make sense sequentially - starting from an empty app, the first test must be creating a page (L4), then subsequent tests can modify existing pages (L1/L2/L3). Let me restructure the test cases to follow a logical progression.
对，应用从空开始，必须先建页面才能改。重新排列：

---

### 10 个测试用例（按执行顺序）

---

#### 第一轮：L4 新建页面（应用为空）

**1. 详细要求 — 普通业务页面**
> 创建客户管理页面：使用已有表 customers（id, name, level, source, owner, last_follow_up, status, note, created_time），顶部3个统计卡片（客户总数/本月新增/待跟进），筛选栏（关键词搜索+客户等级下拉+来源下拉+查询/重置），数据表格（客户名称/等级/来源/负责人/最近跟进/状态），操作列有编辑/删除，新增和编辑用弹窗表单（客户名称必填/等级必填/来源/负责人/备注），删除需确认弹窗，筛选后刷新统计卡片+表格

**验证**：完整8章节分析 → `submit_analysis`（含 `analysisReport`）→ 自动推导 `delegate_query` + `create_code_page` → 确认 → 执行时步骤展开（参考分析报告中的模块、布局、交互联动）

**2. 笼统需求 — 监控大屏**
> 做一个告警监控大屏，使用已有表 alerts（id, alert_name, level, source, location, created_time, status）

**验证**：LLM 应推断地图（告警→地理）、统计卡片、图表、列表，但应追问"需要展示哪些告警指标？地图显示什么？需要实时刷新吗？"

**3. 详细要求 — 流程**
> 创建请假审批流程：表单字段为请假类型（年假/事假/病假/调休，必填）、起止日期（日期范围，必填）、请假天数（数字，必填）、请假原因（多行文本，必填）；审批流程为请假天数≤3天→直属上级审批，>3天→直属上级审批→部门经理审批；支持驳回退回发起人、加签

**验证**：完整分析 → 自动推导 `delegate_workflow(design_form)` + `delegate_workflow(design_workflow)`，流程步骤依赖表单步骤，不创建页面步骤

---

#### 第二轮：L2 数据调整（基于用例1已建的客户管理页面）

**4. 详细要求**
> 在客户管理页面增加"客户来源"饼图（展示各来源占比），点击饼图扇区时筛选栏自动选中对应来源并刷新表格

**验证**：简化分析（第5+7+8章）→ `submit_analysis` → 需修改查询加来源字段 + 更新页面加饼图+交互联动（饼图点击→筛选）

**5. 笼统需求**
> 客户页面再加几个筛选条件

**验证**：LLM 应追问"需要加哪些筛选？按什么字段筛选？"

---

#### 第三轮：L3 页面改造（基于用例2已建的告警监控大屏）

**6. 详细要求**
> 在告警监控大屏，把静态指标卡片改成每30秒自动刷新，增加"告警趋势"折线图（近7天每日告警数），点击折线图某个点时下方表格自动筛选到该日期的告警记录

**验证**：中等分析（第2+4+5+7+8章）→ 涉及新增查询 + 更新页面 + 交互联动（折线图点击→表格筛选）→ `analysisReport` 包含交互联动细节

**7. 笼统需求**
> 告警大屏功能太简单了，丰富一下

**验证**：LLM 应追问"想增加哪类功能？图表？筛选？详情下钻？还是实时刷新？"

---

#### 第四轮：L1 单点修改（基于已有页面）

**8. 详细要求**
> 把客户管理页面的统计卡片背景色从蓝色改成绿色，卡片标题字号从 14px 改为 16px

**验证**：直接调 `update_code_page`，不改数据，不建计划

**9. 笼统需求**
> 首页样式不太好看，调整一下

**验证**：LLM 应追问"哪个页面？哪些地方不好看？想要什么风格？"

---

#### 第五轮：L3 混合场景（流程+页面联动）

**10. 详细要求**
> 创建报销审批流程：使用已有表 expenses（id, type, amount, reason, applicant, status, approval_progress, created_time），表单字段为报销类型（差旅/办公/招待，必填）、金额（数字，必填）、报销事由（多行文本，必填）；审批流程为金额≤5000→直属上级审批→完成，>5000→直属上级→部门经理→财务确认→完成；同时创建"我的报销"页面展示报销记录列表，支持查看审批进度

**验证**：完整分析 → 自动推导 `delegate_workflow(design_form)` + `delegate_workflow(design_workflow)` + `delegate_query` + `create_code_page`，流程依赖表单，页面依赖查询

---

### 执行顺序与依赖

```
用例1 (L4 新建客户管理) ──→ 用例4 (L2 加饼图) ──→ 用例8 (L1 改样式)
用例2 (L4 新建告警大屏) ──→ 用例6 (L3 加折线图) ──→ 用例9 (L1 笼统)
用例3 (L4 新建请假流程)
用例10 (L4 报销流程+页面)
```

### 覆盖矩阵

| 用例 | 级别 | 详细度 | 前置依赖 | 关键验证点 |
|------|------|--------|---------|-----------|
| 1 | L4 | 详细 | 无（首个） | 完整8章节 + analysisReport + 步骤展开 |
| 2 | L4 | 笼统 | 无 | 领域推断（地图）+ 追问 |
| 3 | L4 | 详细 | 无 | 流程步骤依赖 |
| 4 | L2 | 详细 | 用例1 | 简化分析 + 交互联动 |
| 5 | L2 | 笼统 | 用例1 | 追问澄清 |
| 6 | L3 | 详细 | 用例2 | 中等分析 + analysisReport + 步骤展开 |
| 7 | L3 | 笼统 | 用例2 | 追问澄清 |
| 8 | L1 | 详细 | 用例1 | 直接 update，无计划 |
| 9 | L1 | 笼统 | 用例1 | 追问澄清 |
| 10 | L4 | 详细 | 无 | 流程+页面 + 步骤依赖 |

---

## 预制数据

> 测试前先在数据源中执行以下 SQL，创建表并插入预制数据。

### 表1：customers（客户管理 — 用例 1/4/5/8/9）

```sql
CREATE TABLE customers (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT '客户ID',
    name VARCHAR(100) NOT NULL COMMENT '客户名称',
    level VARCHAR(20) NOT NULL COMMENT '客户等级：VIP/高级/普通/潜在',
    source VARCHAR(50) COMMENT '客户来源：官网/推荐/展会/电话/广告',
    owner VARCHAR(50) COMMENT '负责人',
    last_follow_up DATE COMMENT '最近跟进日期',
    status VARCHAR(20) NOT NULL DEFAULT '待跟进' COMMENT '状态：待跟进/跟进中/已成交/已流失',
    note TEXT COMMENT '备注',
    created_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) COMMENT '客户表';

INSERT INTO customers (name, level, source, owner, last_follow_up, status, note) VALUES
('字节跳动', 'VIP', '推荐', '张三', '2026-09-07', '跟进中', '大客户，长期合作'),
('阿里巴巴', 'VIP', '官网', '李四', '2026-09-05', '已成交', '已完成签约'),
('腾讯科技', '高级', '展会', '张三', '2026-09-01', '待跟进', '需重点跟进'),
('华为技术', '高级', '推荐', '王五', '2026-08-28', '跟进中', '二期需求沟通中'),
('小米科技', '普通', '官网', '李四', '2026-08-20', '已流失', '价格未谈拢'),
('京东集团', 'VIP', '电话', '赵六', '2026-09-06', '待跟进', ''),
('美团点评', '普通', '广告', '王五', '2026-08-15', '已成交', ''),
('拼多多', '普通', '展会', '张三', '2026-09-03', '待跟进', '新线索'),
('网易集团', '高级', '推荐', '赵六', '2026-08-25', '跟进中', ''),
('百度在线', 'VIP', '官网', '李四', '2026-09-07', '跟进中', '合同续签'),
('滴滴出行', '高级', '广告', '王五', '2026-08-30', '待跟进', ''),
('快手科技', '普通', '电话', '张三', '2026-09-02', '已成交', ''),
('哔哩哔哩', '普通', '展会', '赵六', '2026-08-18', '已流失', '竞品签约'),
('携程旅行', '高级', '推荐', '李四', '2026-09-04', '待跟进', ''),
('贝壳找房', '普通', '官网', '王五', '2026-08-22', '跟进中', '');

-- 统计验证：共15条，VIP=4，高级=5，普通=6，待跟进=6
```

### 表2：alerts（告警监控 — 用例 2/6/7）

```sql
CREATE TABLE alerts (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT '告警ID',
    alert_name VARCHAR(200) NOT NULL COMMENT '告警名称',
    level VARCHAR(20) NOT NULL COMMENT '告警级别：Critical/Warning/Info',
    source VARCHAR(50) COMMENT '告警来源：服务器/数据库/网络/应用/安全',
    location VARCHAR(50) COMMENT '位置/区域',
    created_time DATETIME NOT NULL COMMENT '产生时间',
    status VARCHAR(20) NOT NULL DEFAULT '未处理' COMMENT '状态：未处理/处理中/已恢复/已忽略'
) COMMENT '告警表';

INSERT INTO alerts (alert_name, level, source, location, created_time, status) VALUES
('CPU使用率超过95%', 'Critical', '服务器', '华北-北京', '2026-09-08 10:30:00', '未处理'),
('磁盘空间不足', 'Warning', '服务器', '华东-上海', '2026-09-08 09:15:00', '处理中'),
('数据库连接池耗尽', 'Critical', '数据库', '华南-广州', '2026-09-08 08:00:00', '已恢复'),
('Nginx 502错误激增', 'Critical', '网络', '华北-北京', '2026-09-08 07:45:00', '未处理'),
('SSL证书即将过期', 'Warning', '安全', '华东-上海', '2026-09-08 06:30:00', '已忽略'),
('API响应超时', 'Warning', '应用', '华北-北京', '2026-09-07 23:00:00', '已恢复'),
('内存使用率超过90%', 'Critical', '服务器', '华南-广州', '2026-09-07 22:15:00', '处理中'),
('登录失败次数异常', 'Info', '安全', '华东-上海', '2026-09-07 21:00:00', '已恢复'),
('Redis内存溢出', 'Warning', '数据库', '华北-北京', '2026-09-07 20:30:00', '未处理'),
('DDoS攻击检测', 'Critical', '安全', '华南-广州', '2026-09-07 19:00:00', '处理中'),
('应用健康检查失败', 'Warning', '应用', '华东-上海', '2026-09-07 18:15:00', '已恢复'),
('防火墙规则变更', 'Info', '网络', '华北-北京', '2026-09-07 17:00:00', '已忽略'),
('K8s Pod频繁重启', 'Critical', '应用', '华南-广州', '2026-09-07 16:30:00', '未处理'),
('MySQL慢查询告警', 'Warning', '数据库', '华东-上海', '2026-09-07 15:00:00', '已恢复'),
('CDN节点故障', 'Info', '网络', '华北-北京', '2026-09-07 14:00:00', '已恢复'),
('日志文件过大', 'Warning', '服务器', '华南-广州', '2026-09-07 12:30:00', '未处理'),
('用户权限异常变更', 'Critical', '安全', '华东-上海', '2026-09-07 11:00:00', '处理中'),
('带宽使用率超过80%', 'Warning', '网络', '华北-北京', '2026-09-07 10:00:00', '已恢复'),
('容器镜像拉取失败', 'Critical', '应用', '华南-广州', '2026-09-07 09:00:00', '已恢复'),
('DNS解析异常', 'Info', '网络', '华东-上海', '2026-09-07 08:00:00', '已忽略'),
('CPU使用率超过90%', 'Warning', '服务器', '华北-北京', '2026-09-06 23:00:00', '已恢复'),
('数据库死锁', 'Critical', '数据库', '华南-广州', '2026-09-06 22:00:00', '已恢复'),
('SSH暴力破解', 'Warning', '安全', '华东-上海', '2026-09-06 20:00:00', '已恢复'),
('服务降级触发', 'Warning', '应用', '华北-北京', '2026-09-06 18:00:00', '已恢复'),
('证书过期', 'Critical', '安全', '华南-广州', '2026-09-06 16:00:00', '已恢复'),
('磁盘IO过高', 'Warning', '服务器', '华东-上海', '2026-09-06 14:00:00', '已恢复'),
('连接数超限', 'Warning', '数据库', '华北-北京', '2026-09-06 12:00:00', '已恢复'),
('网络丢包率过高', 'Critical', '网络', '华南-广州', '2026-09-06 10:00:00', '已恢复');

-- 统计验证：共28条，近7天每天都有数据，级别分布均匀，覆盖5种来源和3个区域
```

### 表3：expenses（报销管理 — 用例 10）

```sql
CREATE TABLE expenses (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT '报销ID',
    type VARCHAR(20) NOT NULL COMMENT '报销类型：差旅/办公/招待',
    amount DECIMAL(10,2) NOT NULL COMMENT '金额',
    reason TEXT NOT NULL COMMENT '报销事由',
    applicant VARCHAR(50) NOT NULL COMMENT '申请人',
    status VARCHAR(20) NOT NULL DEFAULT '审批中' COMMENT '审批状态：审批中/已通过/已驳回',
    approval_progress VARCHAR(200) COMMENT '审批进度',
    created_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) COMMENT '报销表';

INSERT INTO expenses (type, amount, reason, applicant, status, approval_progress) VALUES
('差旅', 3200.00, '北京出差拜访客户，往返机票+住宿', '张三', '审批中', '直属上级审批中'),
('办公', 850.00, '采购办公用品（打印纸、墨盒、文件夹）', '李四', '已通过', '直属上级已审批 → 已完成'),
('招待', 12000.00, '客户来访接待，商务宴请+礼品', '王五', '审批中', '直属上级已审批 → 部门经理审批中'),
('差旅', 1500.00, '上海参加行业展会，往返高铁+住宿', '赵六', '已驳回', '直属上级已驳回（超标）'),
('差旅', 4500.00, '深圳项目驻场，一周住宿+交通', '张三', '审批中', '直属上级已审批 → 部门经理已审批 → 财务确认中'),
('办公', 2300.00, '团队办公设备升级（显示器、键盘）', '李四', '已通过', '直属上级已审批 → 已完成'),
('招待', 3500.00, '合作伙伴商务午餐', '王五', '已通过', '直属上级已审批 → 已完成'),
('差旅', 8000.00, '广州客户现场支持，差旅5天', '赵六', '审批中', '直属上级已审批 → 部门经理审批中'),
('办公', 420.00, '日常办公耗材补充', '张三', '已通过', '直属上级已审批 → 已完成'),
('招待', 6500.00, '项目庆功宴', '李四', '审批中', '直属上级已审批 → 部门经理已审批 → 财务确认中');

-- 统计验证：共10条，覆盖3种类型、4种状态，小额(≤5000)和大额(>5000)均有
```